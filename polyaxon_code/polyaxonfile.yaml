version: 1.1
kind: component
name: train
tags: [examples]

run:
  kind: job
  init:
  - git: {"url": "https://github.com/christiaan-vlist/polyaxon_spike"}
  container:
    image: eu.gcr.io/sandbox-christiaan/polyaxon-spike:latest
    workingDir: "{{ globals.artifacts_path }}/polyaxon_spike"
    command: ["python", "-u", "polyaxon_code"]
